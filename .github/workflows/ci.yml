name: CI
on:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Unit tests + Netlify publish-allowlist build test (Node built-ins only, no npm install needed)
      - run: npm test
